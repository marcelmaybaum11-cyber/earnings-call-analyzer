# CPU-only torch wheels (the CUDA wheels are too large for Streamlit Cloud)
--extra-index-url https://download.pytorch.org/whl/cpu

# Floors instead of exact pins: Streamlit Cloud runs a newer Python
# (3.14) than the local dev venv (3.11), and exact old versions have no
# wheels there. The resolver picks compatible versions per platform.
transformers>=4.40
torch>=2.3
yfinance>=1.4
pandas>=2.2
numpy>=1.26
scikit-learn>=1.4
streamlit>=1.35
plotly>=5.22
statsmodels>=0.14
requests>=2.31
beautifulsoup4>=4.12
